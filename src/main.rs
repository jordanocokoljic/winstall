mod cli;
mod declare_fs;
mod core_fs;

fn main() {
    /*
     * Flow
     * 1. Read command line arguments and environment into some structure
     * 2. Convert this structure into an operation
     * 3. Execute the operation
     */
}
